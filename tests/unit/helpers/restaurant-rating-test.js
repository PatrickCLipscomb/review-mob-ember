import { restaurantRating } from 'review-mob/helpers/restaurant-rating';
import { module, test } from 'qunit';

module('Unit | Helper | restaurant rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restaurantRating([42]);
  assert.ok(result);
});
